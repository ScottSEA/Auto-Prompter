# Domain Docs

Before exploring, read root `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist.
Proceed silently when they do not exist; domain-modeling skills create them lazily.

This is a single-context repository:

```text
/
|-- CONTEXT.md
|-- docs/adr/
`-- source modules
```

Use glossary vocabulary consistently. If a proposed change contradicts an ADR, identify the ADR
and surface the conflict rather than silently overriding it.
